location ^~ /music/ {
    # If it's an mp3 and has NO referer, redirect to exercise page
    if ($http_referer = "") {
        rewrite ^/music/([^.]+)\.mp3$ /?exercise=$1 redirect;
    }

    # Otherwise, serve normally
    proxy_pass $forward_scheme://$server:$port;
}